type User {
  id: Int!
  name: String!
  username: String!
  email: String
  phone: String
  website: String
  address: Address
  company: Company
}

type Query {
  user(id: Int!): User
  users: [User]
}
